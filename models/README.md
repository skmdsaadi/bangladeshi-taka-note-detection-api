# Model weights

Copy the trained YOLOv11 weights from Phase-1 to this directory and name the file:

```text
best.pt
```

The real weight file is not included because it was not available in the connected files. Do not submit fabricated predictions; run the supplied scripts after adding the actual weights.
